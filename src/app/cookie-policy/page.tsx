import { LegalPage } from "@/components/sections/LegalPage";

export default function CookiePolicyPage() {
  return <LegalPage title="Cookie Policy" note="Functional storage supports forms and cookie preferences. Optional analytics load only after consent." />;
}
